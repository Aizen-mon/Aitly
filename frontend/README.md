# Flutter Frontend - AI Assistant for Tally Users

Quick start:

1. Install Flutter SDK: https://flutter.dev/docs/get-started/install
2. From this folder run:

```bash
flutter pub get
flutter run
```

The starter app includes a dashboard screen and a chat assistant screen. Configure the backend base URL in `lib/services/api.dart`.
